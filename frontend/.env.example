# API URL (defaults to http://localhost:8000/api/v1)
VITE_API_URL=http://localhost:8000/api/v1

# Web Push Notifications (VAPID)
# Use the same public key as backend
VITE_VAPID_PUBLIC_KEY=your-vapid-public-key
